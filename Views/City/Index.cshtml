@model IEnumerable<Code1st.Models.Province>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProvinceCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cities)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProvinceCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cities)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProvinceName">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProvinceName">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProvinceName">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
